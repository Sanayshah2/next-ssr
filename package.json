{
  "name": "next-on-netlify-demo",
  "scripts": {
    "dev": "NODE_OPTIONS=\"--openssl-legacy-provider\" next",
    "build": "NODE_OPTIONS=\"--openssl-legacy-provider\" next build",
    "postbuild": "NODE_OPTIONS=\"--openssl-legacy-provider\" next-on-netlify"
  },
  "dependencies": {
    "next": "^9.5.4",
    "next-on-netlify": "^2.6.3",
    "react": "^16.13.1",
    "react-dom": "^16.13.1"
  },
  "devDependencies": {
    "buffer-loader": "^0.1.0"
  }
}
